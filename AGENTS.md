# AGENTS.md
# このファイルは本リポジトリで作業する AI エージェントに対する共通作業ルール・指示を定義します。

## プロジェクト概要
- 本プロジェクト（Geese-in-the-Box）は、AI エージェント（Goose 等）の安全な隔離環境（Docker internal ネットワーク + Squid フォワードプロキシ）を提供するサンドボックスです。

## コーディング・作業ルール
1. **言語設定**: 全ての応答・コードコメント・コミットメッセージ・ドキュメントは日本語を使用してください。
2. **Python ツール・テストの実行方針**:
   - Python のスクリプト実行、パッケージ追加、ユニットテストには必ず **`uv`** を使用してください。
   - ユニットテストの実行は **`make test-unit`** または **`uv run --with pytest --with pytest-asyncio --with httpx --with fastapi pytest tests/`** を使用し、直接 `python` や `pytest` コマンドを叩かないでください。
3. **変更の検証**: コードや設定を変更した後は、必ず検証コマンド（`make test-unit` や構文チェック）を実行して正常動作を確認してください。
4. **外部通信の注意**: 本サンドボックス内ではプロキシでホワイトリスト化されていないドメインへの通信は遮断されます。
5. **秘密情報の取り扱い**: `.env` や秘密鍵等を含む設定ファイルのコミットやログ出力は禁止します。
